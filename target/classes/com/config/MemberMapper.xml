<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="MemberMapper">

<select id="selectMyAttendedRaffle" parameterType = "String" resultType = "ResellRDTO">
select *
from resell_R
where resell_rno in (
					select resell_rno
					from rlist
					where memberno = #{memberno}
					)
</select>
</mapper>